{% extends 'base.html.twig' %}

{% block title %}Demande{% endblock %}

{% block body %}
    <h1>Demande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ demande.id }}</td>
            </tr>
            <tr>
                <th>Materiel</th>
                <td>{{ demande.materiel }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ demande.quantite }}</td>
            </tr>
            <tr>
                <th>Date_demande</th>
                <td>{{ demande.dateDemande ? demande.dateDemande|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Temps_demande</th>
                <td>{{ demande.tempsDemande ? demande.tempsDemande|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ demande.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('demande_index') }}">back to list</a>

    <a href="{{ path('demande_edit', {'id': demande.id}) }}">edit</a>

    {{ include('demande/_delete_form.html.twig') }}
{% endblock %}
